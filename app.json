{
  "name": "uri-qr-generator",
  "description": "An HTTP API QR code generator",
  "repository": "https://github.com/orestes/uri-qr-generator",
  "keywords": ["qr", "uri", "generator"],
  "scripts": {
    "postdeploy": "npm install"
  },
  "env": {
    "HTTP_PORT": {
      "description": "Where the HTTP server should listen.",
      "value": "8080"
    },
    "URI_PREFIX": {
      "description": "A prefix prepended to the all the URIs",
      "value": ""
    },
    "PARAM_NAME": {
      "description": "The `GET` param name containing the URI to encode as a QR Code.",
      "value": "uri"
    }
  }

}
